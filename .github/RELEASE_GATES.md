# Required production checks

Before merging a production change to `master`, require the following GitHub checks once branch protection is enabled in repository settings:

- `validate`
- `browser-smoke`
- `authenticated-workspace`

The detailed production and rollback procedure is in `R9_RELEASE_CHECKLIST.md`.
