import { StatusReportHistory } from '@/components/common/workspace/status-report-history';
import MainLayout from '@/components/layout/main-layout';

export default function StatusHistoryPage() {
   return (
      <MainLayout>
         <StatusReportHistory />
      </MainLayout>
   );
}
